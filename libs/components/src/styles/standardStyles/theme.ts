import breakpoints from "./breakpoints";
import mediaQueries from "./mediaQueries";
import space from "./space";
import { textSize } from "./textStyles";
import { colors } from "./colors";

const theme = {
    colors,
    breakpoints, 
    mediaQueries, 
    space, 
    textSize
}

export default theme;